.header
  .header_left
    = link_to root_path, class: "header_left_title" do
      ToDo_Note
  .header_center
    - if @group.present?
      = "『" + @group.name + "』" + "ノート"
  .header_right
    = link_to edit_user_path(current_user), class: "header_right_edit" do
      %i.fas.fa-users-cog
        %span.fa-users-cog_text
          設定